- name: 'developer group'
  group:
    name: developer
    state: present
- name: 'tino'
  user:
    name: tino
    comment: Developer tino
    shell: /bin/bash
    groups: developer
    append: yes
- name: 'jenkins user'
  user:
    name: jenkins
    comment: Jenkins Automation User
    shell: /bin/bash

